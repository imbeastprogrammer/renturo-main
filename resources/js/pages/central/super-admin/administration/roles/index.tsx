import SuperAdminLayout from '@/layouts/SuperAdminLayout';

function Roles() {
    return <div>Roles</div>;
}

Roles.layout = (page: any) => <SuperAdminLayout>{page}</SuperAdminLayout>;

export default Roles;
