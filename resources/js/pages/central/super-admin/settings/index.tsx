import SuperAdminLayout from '@/layouts/SuperAdminLayout';

function Settings() {
    return <div>Settings</div>;
}

Settings.layout = (page: any) => <SuperAdminLayout>{page}</SuperAdminLayout>;

export default Settings;
