import SuperAdminLayout from '@/layouts/SuperAdminLayout';

function Domains() {
    return <div>Domains</div>;
}

Domains.layout = (page: any) => <SuperAdminLayout>{page}</SuperAdminLayout>;

export default Domains;
